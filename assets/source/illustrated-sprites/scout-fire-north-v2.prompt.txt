Use case: precise-object-edit. Add ONE missing muzzle flash to the supplied Scout firing sprite sheet. TOP ROW, SECOND COLUMN ONLY: add a small bright orange-yellow flash directly at the tip of the vertical carbine muzzle above the rear-facing Scout head. The muzzle projects straight UP/away; flash starts at its upper tip and extends upward roughly12-18 source pixels. Match the restrained flash size/color from second-column other rows. Keep the existing complete carbine muzzle and head unchanged. ALL other15 sprites, all bodies, feet, hands, poncho, ponytail, directions, phase geometry and magenta background must remain unchanged. Keep same exact4x4 layout, no labels or extra objects.
